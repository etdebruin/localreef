node_modules/

# Built output from linked or sample apps — never the source.
apps/*/node_modules/
apps/*/dist/
apps/*/.venv/

*.log
.DS_Store

# Local overrides; the committed defaults are in package.json / eslint.config.js
.env
.env.local

# Vite fixture is generated on demand by npm run test:vite
test/vite/fixture/
